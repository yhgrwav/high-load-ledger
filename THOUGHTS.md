в общем я не определился каким образом буду выражать все свои мысли и обозначать все проблемы, с которыми я столкнулся, так что решил просто писать огромный файл с upd.

## Последовательность разработки проекта:
1. protobuf контракт
2. описание миграции 
3. описание makefile
4. описание dockerfile, docker-compose
5. реализация чтения и валидации .env параметров в конфиг структуру   
6. реализация слоя репозитория: 1) account.go, 2) posting.go, 3) transaction.go
7. рефакторинг proto контракта
8. регенерация pb кода
9. реализация методов полученного интерфейса   



недавно перечитывая свой старый код я обратил внимание на то, что у меня очень-очень упрощённая архитектура, что в каком-то смысле даже имеет смысл, но сейчас у меня появилась явная цель - написать продакшн-лайк сервис, соответственно и архитектура должна быть полноценной. на этапе написания этого сообщения я реализовал миграции, контракт grpc, makefile и прочие незначительные удобства и столкнулся с проблемой: я не понимаю где мне стоит хранить сущности. я сделал ресёрч и понял, что я хочу разделять структуры на следующие типы:
* database model
* domain entity 

сама по себе схема как я понимаю стара как мир, но для меня это в новинку и я решил выделить это в отдельный пункт для себя и возможно для тех, кто вообще это будет читать :/


upd: когда собирал зависимости в main.go, обратил внимание на то, что всё время кидал панику вместо того, чтоб задуматься над os.Exit(1), меня по этому вопросу однажды спросили на собеседовании, а я даже и не думал над этим никогда, щас немного почитал и пересмотрел свой подход.

upd: заметил что не добавил поле updated_at в миграцию, но добавил в структуре кода и встал вопрос где заполнять это поле, по сути своей передавать через сеть, передавая больше байт и тратить ресурсы на маршал и анмаршал(если он вообще там используется) тяжелее, чем создать триггер в базе, который при каждом  обновлении будет обновлять поле, так что пока так, решил подметить просто что нашел такую интересную точку для оптимизации, которая и кода меньше делает и нагрузку снижает.

upd: написал первый метод и хотел бы ещё подметить что придётся писать в 3 раза больше кода для еррор хендлера из-за логгера, т.к. нужно будет и вызвать метод логгера и прокинуть контекст и тело ошибки и какой-то айдишник и кинуть обычную ошибку для враппинга ошибки

upd: возвращаясь к теме триггеров в базе, я обсудил эту тему с senior java dev и пришел к выводу что это антипаттерн и неудобно с точки зрения долгосрочной поддержки кода (коей не будет в рамках этого проекта, но всё же); так что я откачу пожалуй это изменение, ведь и тестироваться будет проще и читаемость улучшится и управляемость повысится и тд и тп, в общем триггер был ошибкой.

upd: дописал сейчас метод изменения баланса у аккаунта, хотел бы отдельно подметить что рассчёт будет на стороне сервиса, т.к. это экономнее по ресурсам и у нас не будет размываться ответственность между базой и сервисом (как в случае с триггерами), а вся ответственность будет лежать именно на стороне сервиса, т.к. мощность у него дешёвая, управляется всё приложение как ни крути из сервиса и меняться если что-то и будет, то будет именно в коде, а не на стороне базы 

upd: обнаружил острую проблему: как распределить права между транзакциями так, чтобы одна транзакция не могла получить права над другой? прогнал пару теорий и пришёл к выводу, что вариант раздутия сущности репозитория не подходит, т.к. это никак не разграничит права, прокидывать идентификатор тоже не вариант, ведь с ним ничего на стороне базы не поделаешь, так что уязвимость останется и есть третий вариант: сделать небольшую обёртку и принимать транзакцию как аргумент. этот вариант вроде как и будет идентификатором и обработку ошибок сходу понятно как сделать.  

upd: ещё хотелось бы отдельно отметить непосредственно метод интеграции кастомной транзакции как сущности в методы базы, я считаю конструкцию pgTx, ok := tx.(pgx.Tx) интересным решением, дошёл до него не сам, но суть ясна: вытянуть из типа any конкретный тип, и если он пустой или не соответствует - вернуть кастомную ошибку. в этой реализации технически скрывается выбор базы и библиотеки, что реализовывает какой-то там принцип ооп.

upd: впервые захожу дальше, чем просто описание protobuf контракта, вроде тема простая и супер полезная, даже, честно говоря, ожидаю подвох в своей реализации, но вроде после ресёрча с помощью notebooklm узнал всю базу, а дальше после ревью пофикшу недочёты и вроде как мой сервис будет готов. пока что не совсем понимаю каким образом буду генерировать трафик, но думаю что у меня будет ещё один микросервис без архитектуры и сложного устройства, а просто говнокод который будет создавать нагрузку, что и даст понимание реализации клиента и сервера в рамках grpc и возможно эту схему можно будет как-то значительно укрепить если будет время. 

upd: при создании метода для создания аккаунта возникла проблема - какой баланс по дефолту ставить? если думать над этим вопросом больше одной секунды, то можно предположить что аккаунт может создаться в рамках какой-то акции и получить бонус к балансу по дефолту, но это значит что нужно либо писать надстройку, либо полностью рефакторить метод так, чтобы он проверял, зарегистрировался ли юзер в рамках какой-то бонусной акции, или нет, но я пока не совсем понимаю как это технически реализовать, да и я не пишу приложение, а пишу сервис, который не привязан явно в какой-то бизнес-логике, так что оставлю явный ноль  

upd: практически сутки потратил на то, чтоб правильно собрать метод транзакции, всё же не удалось мне полностью без ИИ агентов написать проект и раз уж на то пошло, то я попросил ревью кода и объяснил всю задумку, а на выходе получил интересные способы обработки ошибок через select, что для меня практически в новинку, по итогу один метод вышел под 200 строк, пока его ещё не тестировал, но результат обещает быть очень мощным. Сейчас ещё второй день как пытаюсь понять основы обсервабилити, очень сложно понять как всё сразу между собой должно взаимодействовать, что мне хотелось бы видеть в графане и тд, так что попробую пойти от меньшего и настроить примитивные метрики для prometheus, но есть такое ощущение что не туда куда-то я полез и надо было проектировать всё как-то иначе, а то я попросту не укладываюсь по времени, но пока по итогу вырисовывается такой план: prometheus -> jaeger -> grafana -> kafka -> сервис продюсер нагрузки -> настройка общения между сервисами с помощью kafka, благо простенький опыт и общее теоретическое представление имеется и kafka я прикручу за +- день. Также ещё пришлось немного упростить работу со статусами, так что у меня останется только успех/неуспех/неизвестно, а то мне кажется либо я чего-то не понимаю, либо это будет очень сильно нагружать базу мусорными записями, а база в этом проекте итак ботлнек, да и у меня сам метод трансфера атомарный, так что вообще не знаю зачем я их добавил, просто наверное на этапе проектирования контракта решил перемудрить с енумами по примеру с одного из собеседований.

upd: поглощаю теорию по grafana, сейчас стою перед выбором: настроить всё в юайке или выбрать вариацию observability as code, но мне кажется что все практики связанные с поддержкой кода будут плохо переноситься сохранением настроек в volume докера, так что я всё же сделаю выбор в сторону кода, осталось только найти вменяемую документацию/гайд и добить этот этап, а то уже не осталось ни малейшего желания этим заниматься. 

upd: еле еле со всеми существующими костылями дописал связку grafana-prometheus, в общем с помощью gemini нагенерил дашборды с прикрученными метриками из кода, вроде всё работает, осталось прикрутить jaeger и наконец-то я с этим кошмаром закончу и перейду к интеграции kafka+kubernetes. По поводу kafka я немного посоветовался и решил немного иначе интегрировать эту связку. Примерный концепт следующий: пользователи делают запросы -> запросы распределяются в кафке между подами -> рпс кратно растёт

upd: почитал про jaeger поподробнее и решил что я не готов насиловать свою психику ради непрофильного навыка который скорее всего не имеет никакого смысла в рамках этого проекта, так что я наконец-то завершаю этап интеграции observability в проект и буду переходить к следующим этапам.

upd: попросил у курсора ревью моего кода, получил огромный массив ошибок :)) исправил всё что мог ручками, что не мог попросил дописать самому, подправил и наконец-то по итогу убрал статусы которые не подходили под мой способ реализации транзакций и интегрировал postings, про которые я вообще забыл после проектирования проекта и реалиазции методов для бд, надеюсь больше никогда не буду возвращаться к логике проекта, потому что без явно продуманного и декомпозированного ТЗ это невероятно сложно держать всё в голове и по хорошему нужно потратить несколько дней чтоб всё ручками собрать в .pdf или .md файл и описать все возможные ожидаемые фичи а ещё и по хорошему сверху проревьювить это ТЗ, в общем остаётся самое сложное - связка kafka+kubernetes и сервис нагрузки, думаю может сервис вообще реализовать в этом же проекте просто в маленькой директории, чтоб одним компоузом можно было поднять всё что нужно для итогового результата.