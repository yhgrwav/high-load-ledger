в общем я не определился каким образом буду выражать все свои мысли и обозначать все проблемы, с которыми я столкнулся, так что решил просто писать огромный файл с upd.

недавно перечитывая свой старый код я обратил внимание на то, что у меня очень-очень упрощённая архитектура, что в каком-то смысле даже имеет смысл, но сейчас у меня появилась явная цель - написать продакшн-лайк сервис, соответственно и архитектура должна быть полноценной. на этапе написания этого сообщения я реализовал миграции, контракт grpc, makefile и прочие незначительные удобства и столкнулся с проблемой: я не понимаю где мне стоит хранить сущности. я сделал ресёрч и понял, что я хочу разделять структуры на следующие типы:
* database model
* domain entity 

сама по себе схема как я понимаю стара как мир, но для меня это в новинку и я решил выделить это в отдельный пункт для себя и возможно для тех, кто вообще это будет читать :/


upd: когда собирал зависимости в main.go, обратил внимание на то, что всё время кидал панику вместо того, чтоб задуматься над os.Exit(1), меня по этому вопросу однажды спросили на собеседовании, а я даже и не думал над этим никогда, щас немного почитал и пересмотрел свой подход.

upd: заметил что не добавил поле updated_at в миграцию, но добавил в структуре кода и встал вопрос где заполнять это поле, по сути своей передавать через сеть, передавая больше байт и тратить ресурсы на маршал и анмаршал(если он вообще там используется) тяжелее, чем создать триггер в базе, который при каждом  обновлении будет обновлять поле, так что пока так, решил подметить просто что нашел такую интересную точку для оптимизации, которая и кода меньше делает и нагрузку снижает.

upd: написал первый метод и хотел бы ещё подметить что придётся писать в 3 раза больше кода для еррор хендлера из-за логгера, т.к. нужно будет и вызвать метод логгера и прокинуть контекст и тело ошибки и какой-то айдишник и кинуть обычную ошибку для враппинга ошибки

upd: возвращаясь к теме триггеров в базе, я обсудил эту тему с senior java dev и пришел к выводу что это антипаттерн и неудобно с точки зрения долгосрочной поддержки кода (коей не будет в рамках этого проекта, но всё же); так что я откачу пожалуй это изменение, ведь и тестироваться будет проще и читаемость улучшится и управляемость повысится и тд и тп, в общем триггер был ошибкой.

upd: дописал сейчас метод изменения баланса у аккаунта, хотел бы отдельно подметить что рассчёт будет на стороне сервиса, т.к. это экономнее по ресурсам и у нас не будет размываться ответственность между базой и сервисом (как в случае с триггерами), а вся ответственность будет лежать именно на стороне сервиса, т.к. мощность у него дешёвая, управляется всё приложение как ни крути из сервиса и меняться если что-то и будет, то будет именно в коде, а не на стороне базы 

upd: обнаружил острую проблему: как распределить права между транзакциями так, чтобы одна транзакция не могла получить права над другой? прогнал пару теорий и пришёл к выводу, что вариант раздутия сущности репозитория не подходит, т.к. это никак не разграничит права, прокидывать идентификатор тоже не вариант, ведь с ним ничего на стороне базы не поделаешь, так что уязвимость останется и есть третий вариант: сделать небольшую обёртку и принимать транзакцию как аргумент. этот вариант вроде как и будет идентификатором и обработку ошибок сходу понятно как сделать.  

upd: ещё хотелось бы отдельно отметить непосредственно метод интеграции кастомной транзакции как сущности в методы базы, я считаю конструкцию pgTx, ok := tx.(pgx.Tx) интересным решением, дошёл до него не сам, но суть ясна: вытянуть из типа any конкретный тип, и если он пустой или не соответствует - вернуть кастомную ошибку. в этой реализации технически скрывается выбор базы и библиотеки, что реализовывает какой-то там принцип ооп.